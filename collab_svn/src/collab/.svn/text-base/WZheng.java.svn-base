package collab;

/**
 * @author Wenting
 */
//I see you Wenting :)
public class WZheng {
	@Override
	public String toString() {
		String str = "Wenting Zheng";
		if (Math.random() < 0.95) {
			return ("<" + str + "@_@" + ">"); 
		}
		else {
			return ("<" + str.hashCode() + ">:<");
		}
		// Alice is annoyed.
	}
}
